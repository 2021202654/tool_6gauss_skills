# === 1. 核心科学计算 (Core ML) ===
# 必须 >= 1.6.1 以兼容您生成的 .pkl 模型
scikit-learn>=1.6.1
# ⚠️ 关键设置：锁定 Numpy < 2.0
# 原因：新版 Numpy 2.0 会导致很多旧库报错，锁定在 1.x 版本最安全
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
joblib>=1.3.0
# 如果您的代码没用到 xgboost，可以注释掉下面这行；用到则保留
xgboost>=2.0.0

# === 2. AI Agent 框架 (LangChain 0.2.x 稳定版) ===
# 基于您刚才成功安装的版本，我们锁定在 0.2 系列
langchain>=0.2.14,<0.3.0
langchain-community>=0.2.12,<0.3.0
langchain-core>=0.2.43,<0.3.0
langchain-openai>=0.1.23,<0.2.0
langchain-text-splitters>=0.2.4,<0.3.0

# === 3. Web 界面 ===
streamlit>=1.32.0

# === 4. 依赖冲突防御 (Conflict Guard) ===
# 限制 Protobuf 版本，防止 Streamlit 报错
protobuf>=4.25.0,<5.0.0
# 如果未来用到 shap，限制版本以配合 Numpy 1.x

shap<0.50.0

matplotlib
scipy
